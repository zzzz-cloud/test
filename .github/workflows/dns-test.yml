name: DNS Lookup
on: [push, workflow_dispatch]  # 支持推送和手动触发

jobs:
  kdig-test:
    runs-on: ubuntu-latest
    steps:
      - name: Install kdig
        run: |
          sudo apt-get update
          sudo apt-get install -y knot-dnsutils

      # =========================
      # 🔹 传统 UDP/TCP DNS 查询
      # =========================
      - name: Cloud(1.1.1.1)
        run: kdig @1.1.1.1 y80s.net
      - name: Google DNS (8.8.8.8)
        run: kdig @8.8.8.8 y80s.net
      - name: test
        run: dig +dnssec qwe68.cn @8.8.8.8
      - name: Quad9 DNS (9.9.9.9)
        run: kdig @9.9.9.9 y80s.net

      - name: OpenDNS (208.67.222.222)
        run: kdig @208.67.222.222 y80s.net

      - name: AdGuard DNS (94.140.14.14)
        run: kdig @94.140.14.14 y80s.net

      - name: Alibaba DNS (223.5.5.5)
        run: kdig @223.5.5.5 y80s.net

      - name: DNSPod (119.29.29.29)
        run: kdig @119.29.29.29 y80s.net

      # =========================
      # 🔹 DNS over TLS (DoT)
      # =========================
      - name: Cloudflare DNS over TLS (+tls)
        run: kdig @1.1.1.1 +tls y80s.net

      - name: Cloudflare DNS over TLS (tls://)
        run: kdig tls://1.1.1.1 y80s.net

      - name: Google DNS over TLS (+tls)
        run: kdig +tls @dns.google y80s.net

      - name: Google DNS over TLS (tls://)
        run: kdig tls://dns.google y80s.net

      - name: Quad9 DNS over TLS (+tls)
        run: kdig @9.9.9.9 +tls y80s.net

      - name: Quad9 DNS over TLS (tls://)
        run: kdig tls://dns.quad9.net y80s.net

      - name: AdGuard DNS over TLS (+tls)
        run: kdig @94.140.14.14 +tls y80s.net

      - name: AdGuard DNS over TLS (tls://)
        run: kdig tls://unfiltered.adguard-dns.com y80s.net

      # =========================
      # 🔹 DNS over HTTPS (DoH)
      # =========================
      - name: Cloudflare DoH
        run: kdig https://cloudflare-dns.com/dns-query y80s.net

      - name: Google DoH
        run: kdig https://dns.google/dns-query y80s.net

      - name: Google Doh-2
        run: kdig +https=https://dns.google/dns-query y80s.net
      - name: Google Doh-3
        run: kdig @https://dns.google y80s.net

      - name: Quad9 DoH
        run: kdig https://dns.quad9.net/dns-query y80s.net

      - name: AdGuard DoH
        run: kdig https://unfiltered.adguard-dns.com/dns-query y80s.net
